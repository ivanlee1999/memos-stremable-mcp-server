# Flomo MCP Server Configuration

# Required: Your Flomo bearer token (get from flomoapp.com after logging in)
FLOMO_BEARER_TOKEN=your_bearer_token_here

# Optional: Flomo API configuration
FLOMO_BASE_URL=https://flomoapp.com
FLOMO_WEBHOOK_URL=
FLOMO_TIMEOUT=30
FLOMO_MAX_RETRIES=3

# Optional: Server configuration
SERVER_HOST=localhost
SERVER_PORT=8000
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
API_RATE_LIMIT=100

# Instructions:
# 1. Get your bearer token:
#    - Go to flomoapp.com and log in
#    - Open browser developer tools (F12)
#    - Go to Network tab
#    - Make any request (like refreshing the page)
#    - Look for the Authorization header with "Bearer xxxxxx"
#    - Copy the token (without "Bearer ")
# 2. Copy this file to .env: cp .env.example .env
# 3. Replace "your_bearer_token_here" with your actual token
# 4. Adjust other settings as needed