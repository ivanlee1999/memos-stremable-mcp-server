# Memos MCP Server Configuration

# Required: Your Memos instance URL and access token
MEMOS_BASE_URL=https://your-memos-instance.com
MEMOS_ACCESS_TOKEN=your_access_token_here

# Optional: API configuration
MEMOS_API_VERSION=v1
MEMOS_TIMEOUT=30
MEMOS_MAX_RETRIES=3

# Optional: Server configuration
SERVER_HOST=localhost
SERVER_PORT=8000
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
API_RATE_LIMIT=100

# Instructions for Memos setup:
# 1. Get your Memos access token:
#    - Go to your Memos instance (e.g., https://demo.usememos.com)
#    - Log in to your account
#    - Go to Settings page
#    - Click "Create" button in Access Tokens section
#    - Provide a description (e.g., "MCP Server")
#    - Set expiration date
#    - Copy the generated token
# 2. Set your Memos instance URL:
#    - Replace "https://your-memos-instance.com" with your actual URL
#    - For self-hosted: http://localhost:5230 (default port)
#    - For demo: https://demo.usememos.com
# 3. Copy this file to .env: cp .env.memos.example .env
# 4. Replace the placeholder values with your actual configuration